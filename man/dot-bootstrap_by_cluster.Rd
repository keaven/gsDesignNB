% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiple_imputation_nb.R
\name{.bootstrap_by_cluster}
\alias{.bootstrap_by_cluster}
\title{Bootstrap-resample subjects within strata (internal)}
\usage{
.bootstrap_by_cluster(data, subject_col, strata_cols, n_boot)
}
\arguments{
\item{data}{Data frame to resample.}

\item{subject_col}{Character. Column that identifies subjects (clusters).}

\item{strata_cols}{Character vector. Stratification column names. May be
\code{character(0)} for no stratification.}

\item{n_boot}{Integer. Number of bootstrap replicates.}
}
\value{
List of length \code{n_boot}. Each element is a copy of \code{data} with an
added \code{replicate} integer column and (when \code{n_boot > 1}) potentially
modified \code{subject_col} values for duplicated subjects.
}
\description{
Performs stratified cluster (subject-level) bootstrap resampling. When a
subject is drawn more than once its rows are duplicated and the
\code{subject_col} values are replaced with unique pseudo-IDs so that the GLMM
treats them as separate individuals (mirroring SAS \verb{PROC SURVEYSELECT} with
\verb{method=urs cluster=USUBJID}).
}
\keyword{internal}
