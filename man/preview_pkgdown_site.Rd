% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preview_pkgdown_site.R
\name{preview_pkgdown_site}
\alias{preview_pkgdown_site}
\title{Preview built pkgdown site in the browser}
\usage{
preview_pkgdown_site(port = 8787L)
}
\arguments{
\item{port}{Integer; TCP port for the static server (default \code{8787}).}
}
\value{
Invisibly, \code{NULL} (called for its side effect of running the server).
}
\description{
\code{pkgdown::preview_site()} opens \code{docs/index.html} as a \verb{file://} URL. Many
browsers restrict loading stylesheets, scripts, and fonts from local files, so
the site can appear almost unstyled. This function serves \verb{docs/} over HTTP
on the loopback interface so the site matches the published GitHub Pages
appearance.
}
\details{
Call from the \strong{package root} after \code{\link[pkgdown:build_site]{pkgdown::build_site()}} (or any build
that populates \verb{docs/}).

The server runs until you interrupt the R session (e.g. \strong{Esc} in RStudio or
Ctrl+C in the terminal).
}
\examples{
\dontrun{
pkgdown::build_site()
preview_pkgdown_site()
}

}
