% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiple_imputation_nb.R
\name{impute_nb}
\alias{impute_nb}
\title{Multiple imputation for longitudinal negative binomial counts}
\usage{
impute_nb(
  data,
  formula,
  outcome_col,
  miss_flag_col,
  baseline_col,
  trt_col,
  reference_trt,
  subject_col,
  strata_cols = NULL,
  mar_values = "MAR",
  mnar_value = "MNAR",
  composite_value = "Comp",
  n_imp = 5L,
  n_boot = 1L,
  seed = NULL
)
}
\arguments{
\item{data}{Data frame in long format (one row per
subject × visit).}

\item{formula}{Two-sided formula passed to \code{\link[=fit_nb_glmm]{fit_nb_glmm()}},
specifying fixed and random effects. The left-hand side should be the
outcome variable (with \code{NA} for missing observations). Example:
\code{count ~ baseline + trt + visit + (1 | id)}.}

\item{outcome_col}{Character. Column name of the count outcome.}

\item{miss_flag_col}{Character. Column name of the missingness mechanism
flag. Values in this column control which imputation strategy is applied:
\code{mar_values}, \code{mnar_value}, or \code{composite_value}. Rows with \code{NA} in this
column are treated as complete (observed).}

\item{baseline_col}{Character. Column name of the baseline count used by
the composite strategy.}

\item{trt_col}{Character. Column name of the treatment group.}

\item{reference_trt}{Value in \code{trt_col} identifying the reference
(comparator) arm.}

\item{subject_col}{Character. Column name of the subject identifier
(cluster unit for bootstrap resampling).}

\item{strata_cols}{Character vector of column names used to stratify the
bootstrap resampling. Default \code{NULL} (no stratification).}

\item{mar_values}{Character vector. Values of \code{miss_flag_col} treated as
MAR. Default \code{"MAR"}.}

\item{mnar_value}{Character. Value of \code{miss_flag_col} treated as MNAR
(triggers reference-based imputation for non-reference arms). Default
\code{"MNAR"}.}

\item{composite_value}{Character. Value of \code{miss_flag_col} that triggers the
composite strategy (baseline carry-forward for missing rows). Default
\code{"Comp"}.}

\item{n_imp}{Integer. Number of imputations per bootstrap
replicate. Default \code{5L}.}

\item{n_boot}{Integer. Number of bootstrap replicates. Default
\code{1L} (no resampling; a single GLMM is fitted to the original data).}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility.
Default \code{NULL}.}
}
\value{
A data frame with all columns from \code{data} plus:
\describe{
\item{\code{replicate}}{Bootstrap replicate index (1 to \code{n_boot}).}
\item{\code{imputation}}{Imputation index (1 to \code{n_imp}).}
\item{\code{imputed_value}}{Imputed count. Equals the observed value for
non-missing rows; contains imputed draws for missing rows.}
}
The total number of rows is
\code{nrow(data) * n_boot * n_imp}.
}
\description{
Orchestrates the full multiple imputation (MI) pipeline for longitudinal
recurrent-event count data with negative binomial overdispersion:
}
\details{
\enumerate{
\item \strong{Bootstrap resampling} (optional): cluster-level (subject-level)
stratified resampling with replacement, creating \code{n_boot} replicates.
This propagates estimation uncertainty into the imputed values,
mirroring the \verb{PROC SURVEYSELECT method=urs cluster=USUBJID} step in the
SAS macro.
\item \strong{GLMM fitting}: a negative binomial GLMM is fitted to the observed
(non-missing) rows of each replicate via \code{\link[=fit_nb_glmm]{fit_nb_glmm()}}.
\item \strong{Imputation by mechanism}:
\itemize{
\item \emph{MAR} rows: predicted mean with subject BLUPs → Gamma–Poisson draw.
\item \emph{MNAR reference-arm} rows: same as MAR (reference arm has no
"better" treatment to copy from).
\item \emph{MNAR non-reference-arm} rows: reference-based (copy-reference)
imputation. The counterfactual mean is the fixed-effects-only
prediction under the reference arm multiplied by the subject's
random-effect ratio (BLUP prediction / FE prediction on the response
scale). See \code{\link[=impute_nb_mnar_ref]{impute_nb_mnar_ref()}}.
\item \emph{Composite ICE} rows: missing value set to baseline count.
See \code{\link[=impute_nb_composite]{impute_nb_composite()}}.
}
\item Returns a long-format data frame with one row per original observation ×
bootstrap replicate × imputation.
}
\subsection{Relationship between bootstrap and MI}{

Setting \code{n_boot > 1} combines bootstrap and MI ("boot-MI"), which yields
a valid variance estimator without requiring Rubin's rules. Setting
\code{n_boot = 1} produces conventional MI; apply Rubin's rules to the \code{n_imp}
imputed datasets when pooling.
}

\subsection{Formula and GLMM specification}{

The formula is passed directly to \code{glmmTMB::glmmTMB()}. A typical formula
mirrors the PROC GLIMMIX model:

\if{html}{\out{<div class="sourceCode">}}\preformatted{outcome ~ baseline + strat1 + strat2 + trt + visit + param + (1 | id)
}\if{html}{\out{</div>}}

The original SAS model also included an unstructured residual covariance
across visits within \code{id:param}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{+ (0 + visit | id:param)
}\if{html}{\out{</div>}}

Complex random-effect structures may cause convergence issues; start with
a random intercept only and add complexity as needed.
}

\subsection{Composite strategy}{

The composite strategy applies only to \strong{missing} post-ICE rows
(\code{is.na(outcome_col)} must be \code{TRUE}). Observed rows with \code{miss_flag_col == composite_value} are left unchanged.
}
}
\examples{
\dontrun{
# Requires glmmTMB
result <- impute_nb(
  data          = long_data,
  formula       = count ~ baseline + trt + visit + (1 | id),
  outcome_col   = "count",
  miss_flag_col = "miss_flag",
  baseline_col  = "baseline",
  trt_col       = "trt",
  reference_trt = 0L,
  subject_col   = "id",
  strata_cols   = c("trt", "strat1"),
  mar_values    = "MAR",
  mnar_value    = "MNAR",
  composite_value = "Comp",
  n_imp         = 5L,
  n_boot        = 10L,
  seed          = 42L
)
head(result[!is.na(result$miss_flag), ])
}
}
