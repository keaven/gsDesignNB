% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiple_imputation_nb.R
\name{impute_nb_mnar_ref}
\alias{impute_nb_mnar_ref}
\title{Impute missing counts under a reference-based MNAR assumption}
\usage{
impute_nb_mnar_ref(
  data,
  fits,
  outcome_col,
  miss_flag_col,
  mnar_value = "MNAR",
  trt_col,
  reference_trt,
  n_imp = 5L,
  replicate_col = NULL
)
}
\arguments{
\item{data}{Data frame including all rows (observed and missing).}

\item{fits}{Named list of fits as returned by \code{\link[=fit_nb_glmm]{fit_nb_glmm()}}.}

\item{outcome_col}{Character. Column with the count outcome.}

\item{miss_flag_col}{Character. Column with the missingness flag.}

\item{mnar_value}{Character. Flag value identifying MNAR rows. Default
\code{"MNAR"}.}

\item{trt_col}{Character. Column with the treatment assignment.}

\item{reference_trt}{Value in \code{trt_col} that identifies the reference arm.}

\item{n_imp}{Integer. Number of imputations per replicate. Default
\code{5}.}

\item{replicate_col}{Character or \code{NULL}. Replicate identifier column.}
}
\value{
Data frame in long format with all original columns plus
\code{imputation} and \code{imputed_value}. Only MNAR non-reference rows have
counterfactual imputations; all other rows pass through unchanged.
}
\description{
Implements the \strong{copy-reference} (CR) strategy for observations whose
missingness flag matches \code{mnar_value} in non-reference treatment arms.
The imputation mean is the fixed-effects-only prediction under the reference
arm, adjusted upward (or downward) by the subject's estimated random effect:
\deqn{\hat\mu_i^{\text{cf}} =
  \hat\mu_i^{\text{FE, ref}} \times
  \frac{\hat\mu_i^{\text{BLUP}}}{\hat\mu_i^{\text{FE}}}.}
This mirrors the SAS \verb{PROC PLM} approach that re-predicts under the
counterfactual treatment and then multiplies by the BLUP ratio on the
response scale.
}
\details{
MNAR subjects already in the reference arm should be handled by
\code{\link[=impute_nb_mar]{impute_nb_mar()}} (MAR imputation is appropriate for the reference arm
because there is no better arm to "copy from").
}
\examples{
\dontrun{
fits     <- fit_nb_glmm(obs_data, count ~ base + trt + visit + (1 | id))
imp_mnar <- impute_nb_mnar_ref(
  data          = long_data,
  fits          = fits,
  outcome_col   = "count",
  miss_flag_col = "miss_flag",
  mnar_value    = "MNAR",
  trt_col       = "trt",
  reference_trt = 0L,
  n_imp         = 5L
)
}
}
