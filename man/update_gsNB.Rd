% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsNBCalendar.R
\name{update_gsNB}
\alias{update_gsNB}
\title{Update group sequential bounds with observed information}
\usage{
update_gsNB(design, observed_info, spending_time = NULL)
}
\arguments{
\item{design}{A \code{gsNB} or \code{gsDesign} object produced by
\code{\link[=gsNBCalendar]{gsNBCalendar()}} (or \code{\link[gsDesign:gsDesign]{gsDesign::gsDesign()}}).}

\item{observed_info}{Numeric vector of observed statistical information at
each analysis conducted so far.
Its length must be between 1 and \code{design$k}.
If shorter than \code{design$k}, information at future analyses is projected
from the planned design.}

\item{spending_time}{Optional numeric vector the same length as
\code{observed_info} giving the spending time (between 0 and 1) for each
analysis.  When \code{NULL}, spending time equals the information fraction.
If shorter than \code{design$k}, future spending times are taken from the
planned design.}
}
\value{
A list with components:
\describe{
\item{design}{The updated \code{gsDesign} object with recalculated
boundaries.}
\item{bounds}{A data frame from \code{\link[gsDesign:gsBoundSummary]{gsDesign::gsBoundSummary()}}
showing Z-boundaries, nominal p-values, approximate treatment effects
at the boundary, and cumulative crossing probabilities at each
analysis.}
\item{info}{A data frame with one row per analysis containing the
information fraction (\code{IF}), spending time (\code{spending_time}),
upper and lower Z-boundaries, and cumulative upper and lower
spending.}
}
}
\description{
Given a planned \code{gsNB} (or \code{gsDesign}) object and observed statistical
information at one or more analyses, recompute the group sequential
boundaries and return an updated design object together with a
\code{\link[gsDesign:gsBoundSummary]{gsDesign::gsBoundSummary()}}-style table.
}
\details{
The observed information determines the covariance structure of the test
statistics (via the information fraction \code{timing}), while \code{spending_time}
controls how much of the error-spending budget has been used.
When \code{spending_time} is \code{NULL} (the default), spending is driven by the
observed information fraction.  Supplying an explicit \code{spending_time} is
useful when the monitoring charter specifies calendar-driven spending that
differs from the observed information fraction.
}
\examples{
library(gsDesign)
nb_ss <- sample_size_nbinom(
  lambda1 = 0.5, lambda2 = 0.3, dispersion = 0.1, power = 0.9,
  accrual_rate = 10, accrual_duration = 20, trial_duration = 24
)
gs <- gsNBCalendar(nb_ss, k = 3, analysis_times = c(12, 18, 24))

# After observing information at the first interim
upd <- update_gsNB(gs, observed_info = gs$n.I[1] * 0.95)
upd$bounds
upd$info
}
