% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiple_imputation_nb.R
\name{fit_nb_glmm}
\alias{fit_nb_glmm}
\title{Fit a negative binomial GLMM for count imputation}
\usage{
fit_nb_glmm(data, formula, replicate_col = NULL)
}
\arguments{
\item{data}{Data frame of \strong{observed} (non-missing) records.}

\item{formula}{A two-sided formula specifying fixed and random effects, e.g.
\code{count ~ base + trt + visit + (1 | id)}. The left-hand side should be the
outcome variable; rows with a missing outcome should be excluded before
calling this function (see \code{\link[=impute_nb]{impute_nb()}}).}

\item{replicate_col}{Character or \code{NULL}. Column in \code{data} identifying
bootstrap replicates. The model is fitted separately within each unique
value. If \code{NULL} (default), all rows form a single fit.}
}
\value{
A named list—one element per unique replicate—where each element
contains:
\describe{
\item{\code{model}}{The fitted \code{glmmTMB} object.}
\item{\code{k}}{Estimated NB dispersion \eqn{k} (= \code{glmmTMB::sigma(model)}).}
}
When \code{replicate_col = NULL}, the list has a single element named \code{"1"}.
}
\description{
Fits a negative binomial generalized linear mixed model (GLMM) to
longitudinal count data using \code{glmmTMB::glmmTMB()} with
\code{family = nbinom2(link = "log")}. The fitted model and the estimated
dispersion parameter \eqn{k} (where Var(Y) = \eqn{\mu + k\mu^2}) are
returned for use in subsequent imputation steps. This mirrors PROC GLIMMIX
with \verb{dist=negbin link=log} in SAS.
}
\details{
The dispersion parameter follows the parameterisation used throughout
\strong{gsDesignNB}: \eqn{k} such that \eqn{\text{Var}(Y) = \mu + k\mu^2}. This
matches \code{glmmTMB}'s \code{nbinom2} family, where \code{glmmTMB::sigma()} returns
\eqn{k} directly.
}
\examples{
\dontrun{
# Requires glmmTMB
obs_data <- long_data[!is.na(long_data$count), ]
fits <- fit_nb_glmm(
  data    = obs_data,
  formula = count ~ baseline + trt + visit + (1 | id)
)
fits[["1"]]$k  # estimated dispersion
}
}
