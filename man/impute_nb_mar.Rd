% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiple_imputation_nb.R
\name{impute_nb_mar}
\alias{impute_nb_mar}
\title{Impute missing counts under Missing at Random (MAR)}
\usage{
impute_nb_mar(
  data,
  fits,
  outcome_col,
  miss_flag_col,
  mar_values = "MAR",
  n_imp = 5L,
  replicate_col = NULL
)
}
\arguments{
\item{data}{Data frame including all rows (observed and missing).}

\item{fits}{Named list of fits as returned by \code{\link[=fit_nb_glmm]{fit_nb_glmm()}}.}

\item{outcome_col}{Character. Column with the count outcome (may have \code{NA}).}

\item{miss_flag_col}{Character. Column with the missingness flag.}

\item{mar_values}{Character vector. Flag values treated as MAR. Default
\code{"MAR"}. In the reference-based framework, reference-arm MNAR subjects are
also imputed as MAR (pass \code{c("MAR", "MNAR")} if desired).}

\item{n_imp}{Integer. Number of imputations per replicate. Default \code{5}.}

\item{replicate_col}{Character or \code{NULL}. Replicate identifier column.}
}
\value{
Data frame in long format with all original columns plus
\code{imputation} (integer 1 to \code{n_imp}) and \code{imputed_value} (imputed count;
equals the observed value for non-missing rows).
}
\description{
For observations whose missingness flag matches \code{mar_values}, generates
\code{n_imp} imputed counts using the GLMM predicted mean \strong{including
subject-level BLUPs}. Draws use the Gamma–Poisson compound distribution:
\deqn{\lambda_i \sim \text{Gamma}(1/k,\; \hat\mu_i^{\text{BLUP}} k),
  \quad Y_i^{(m)} \mid \lambda_i \sim \text{Poisson}(\lambda_i).}
}
\details{
This function handles one or more bootstrap replicates when a
\code{replicate_col} is provided and \code{fits} contains one model per replicate.
Observed rows (non-missing outcome) are passed through unchanged
(\code{imputed_value} = observed value).
}
\examples{
\dontrun{
fits    <- fit_nb_glmm(obs_data, count ~ base + trt + visit + (1 | id))
imp_mar <- impute_nb_mar(
  data          = long_data,
  fits          = fits,
  outcome_col   = "count",
  miss_flag_col = "miss_flag",
  mar_values    = "MAR",
  n_imp         = 5L
)
}
}
