% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_ssr_nbinom.R
\name{sim_ssr_nbinom}
\alias{sim_ssr_nbinom}
\title{Simulate adaptive group sequential trials with sample size re-estimation}
\usage{
sim_ssr_nbinom(
  n_sims,
  enroll_rate,
  fail_rate,
  dropout_rate = NULL,
  max_followup,
  design,
  n_max = NULL,
  strategies = c("No adaptation", "Blinded SSR", "Unblinded SSR"),
  adapt_analysis = NULL,
  min_if_futility = 0,
  max_enrollment_frac_for_adapt = 1,
  min_months_to_close_for_adapt = 0,
  analysis_lag_months = 0,
  event_gap = NULL,
  bound_info = c("unblinded_ml", "blinded_ml", "unblinded_mom", "blinded_mom"),
  first_min_time = 1,
  min_analysis_gap = 0.5,
  ignore_futility = FALSE,
  metadata = NULL,
  test_type = c("wald", "score"),
  seed = TRUE
)
}
\arguments{
\item{n_sims}{Number of simulated trials.}

\item{enroll_rate}{Enrollment-rate data frame passed to \code{\link[=nb_sim]{nb_sim()}}.}

\item{fail_rate}{Failure-rate data frame passed to \code{\link[=nb_sim]{nb_sim()}}. This defines
the data-generating truth.}

\item{dropout_rate}{Optional dropout-rate data frame passed to \code{\link[=nb_sim]{nb_sim()}}.}

\item{max_followup}{Maximum follow-up per patient in the simulated trial.}

\item{design}{A planning object of class \code{gsNB}, typically returned by
\code{\link[=gsNBCalendar]{gsNBCalendar()}} (optionally after \code{\link[=toInteger]{toInteger()}}).}

\item{n_max}{Maximum total enrollment allowed after SSR. Defaults to 150\% of
the planned final enrollment, rounded up and constrained to be at least the
planned sample size.}

\item{strategies}{Character vector of strategies to simulate. Must be chosen
from \code{"No adaptation"}, \code{"Blinded SSR"}, and \code{"Unblinded SSR"}.}

\item{adapt_analysis}{Interim analysis index where SSR may be applied.
Defaults to the last interim analysis (\code{design$k - 1}).}

\item{min_if_futility}{Minimum observed information fraction required before
allowing a futility stop. Default is \code{0}.}

\item{max_enrollment_frac_for_adapt}{Maximum fraction of the planned
enrollment already accrued at the adaptation cut for SSR to be allowed.
Default is \code{1}.}

\item{min_months_to_close_for_adapt}{Minimum predicted months remaining to
planned enrollment close required to allow SSR. Default is \code{0}.}

\item{analysis_lag_months}{Additional months of enrollment counted after a
futility stop to approximate operational lag. Default is \code{0}.}

\item{event_gap}{Optional event-gap duration. If \code{NULL}, inherits
\code{design$nb_design$inputs$event_gap} when available; otherwise defaults to
\code{0}.}

\item{bound_info}{Information measure used for efficacy/futility bounds.
Choices are \code{"unblinded_ml"} (default), \code{"blinded_ml"},
\code{"unblinded_mom"}, and \code{"blinded_mom"}.}

\item{first_min_time}{Minimum calendar time allowed for the first
information-based interim search. Default is \code{1}.}

\item{min_analysis_gap}{Minimum gap between successive information-based
interim searches. Default is \code{0.5}.}

\item{ignore_futility}{Logical. If \code{TRUE}, lower-bound crossings do not stop
the trial.}

\item{metadata}{Optional named list or one-row data frame of scenario labels
to repeat across the returned rows.}

\item{test_type}{Type of test statistic passed to \code{\link[=mutze_test]{mutze_test()}}:
\code{"wald"} (default) or \code{"score"}. See \code{\link[=mutze_test]{mutze_test()}} for details.}

\item{seed}{Random-seed control passed to \code{future.apply::future_lapply()}.
Follows the same conventions as \code{\link[=sim_gs_nbinom]{sim_gs_nbinom()}}.}
}
\value{
An object of class \code{sim_ssr_nbinom} with components:
\describe{
\item{trial_results}{One row per simulation and strategy, containing
trial-level outcomes and stage-specific summaries in wide format.}
\item{analysis_results}{One row per simulation, strategy, and analysis in
long format.}
\item{settings}{A list of key design and simulation settings.}
}
}
\description{
Simulates recurrent-event group sequential trials with information-based
interim analyses and optional sample size re-estimation (SSR). Interim timing
follows the blinded-information targeting used in the SSR study vignette:
\code{\link[=get_cut_date]{get_cut_date()}} searches for the earliest cut date where the blinded
information reaches the planned information fraction, with an unblinded
fallback if the blinded fit is unavailable.
}
\details{
The function compares one or more strategies:
\describe{
\item{"No adaptation"}{The trial keeps the planned sample size.}
\item{"Blinded SSR"}{A blinded nuisance-parameter update is applied at
\code{adapt_analysis} using \code{\link[=blinded_ssr]{blinded_ssr()}}.}
\item{"Unblinded SSR"}{An unblinded nuisance-parameter update is applied at
\code{adapt_analysis} using \code{\link[=unblinded_ssr]{unblinded_ssr()}}.}
}

The returned \code{trial_results} include stage-specific columns such as
\code{z_ia1}, \code{if_ia1}, \code{ia1_time}, \code{participants_with_events_ia1},
\code{events_observed_ia1}, and analogous columns for later analyses. For the
actual stopping stage, \code{participants_with_events_stop} and
\code{events_observed_stop} summarize the observed burden carried into the final
decision for each simulated trial.
}
\examples{
set.seed(123)
enroll_rate <- data.frame(rate = 12, duration = 6)
fail_rate <- data.frame(
  treatment = c("Control", "Experimental"),
  rate = c(0.6, 0.42),
  dispersion = 0.4
)
dropout_rate <- data.frame(
  treatment = c("Control", "Experimental"),
  rate = c(0.05, 0.05),
  duration = c(12, 12)
)
fixed_design <- sample_size_nbinom(
  lambda1 = 0.6,
  lambda2 = 0.42,
  dispersion = 0.4,
  power = 0.8,
  alpha = 0.025,
  accrual_rate = 12,
  accrual_duration = 6,
  trial_duration = 12,
  max_followup = 6
)
gs_design <- gsNBCalendar(
  fixed_design,
  k = 3,
  test.type = 4,
  alpha = 0.025,
  sfu = sfHSD,
  sfupar = -2,
  sfl = sfHSD,
  sflpar = 1,
  analysis_times = c(4, 8, 12)
)
sim_res <- sim_ssr_nbinom(
  n_sims = 2,
  enroll_rate = enroll_rate,
  fail_rate = fail_rate,
  dropout_rate = dropout_rate,
  max_followup = 6,
  design = gs_design,
  seed = 123
)
names(sim_res)
head(sim_res$trial_results)
}
